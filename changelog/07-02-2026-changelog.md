# Changelog — 07/02/2026

- [20:11] code-change: Completed CLOUD_PLAN.md Phase 0 — root workspaces, backend (Hono/jose/wrangler/vitest-pool-workers), frontend (Vite/React/Tailwind/date-fns/react-router/lucide), test JWKS key pair, `.dev.vars`. Verified `wrangler dev` boots with `DB` binding and placeholder route returns 200.
- [20:30] code-change: Phase 1.1–1.4 TDD'd and green (26 backend tests) — D1 schema migration, org-scoped chore data-access layer with optimistic-concurrency version checks, Hono chore routes, and Access JWT validation middleware (env-driven JWKS URL/audience, fail-closed on JWKS fetch failure).
- [20:52] code-change: Phase 1.5–1.8 TDD'd and green (40 backend tests) — org-scoping middleware (cross-org access returns 404, not 403), `GET /api/me`, admin user-management routes, and `bootstrap-admin` script (tested core + `getPlatformProxy`-based CLI wrapper), verified end-to-end against local D1.
- [21:07] code-change: Phase 1.9 complete — React Router (`/`, `/admin`), NavBar admin-conditional link, AdminPanel (list/add/remove users) with component tests, and a real Playwright e2e suite (JWKS fixture server + `wrangler dev` + Vite dev server, seeded local D1) proving non-admins never see the Admin tab and admins can add a user end-to-end. CLOUD_PLAN.md Phase 0 and Phase 1 are now fully green — every `[A]` item checked, only `[B]` (real Cloudflare account) items remain.
- [21:22] code-change: Added ESLint (flat config, typescript-eslint + react-hooks/react-refresh) and Prettier across backend/frontend/e2e; fixed the one real lint error found (a ref-read-during-render in the seeded `DateNavigationBanner`, rewritten to React's documented "previous render" state pattern) and reformatted 42 files. Wired a local pre-commit hook (`scripts/git-hooks/pre-commit` + lint-staged) installed via a `prepare` script that copies straight into `.git/hooks/` — deliberately not Husky, since Husky's setup writes `core.hooksPath` via `git config`, which is off-limits. Verified the hook both auto-fixes a deliberately bad staged file and passes cleanly on the real changeset.
- [21:31] code-change: Added `.github/workflows/lint.yml` — runs `npm run lint` + `npm run format:check` on `pull_request` `opened`/`synchronize`, mirroring the pre-commit hook. `actions/checkout` and `actions/setup-node` are pinned to commit SHAs (resolved via the GitHub API from their latest release tags: checkout v7.0.0, setup-node v6.4.0), not mutable version tags. Validated the workflow with `actionlint` (clean) and confirmed `npm ci && npm run lint && npm run format:check` passes locally — caught and fixed a small residual formatting drift in `CLOUD_PLAN.md` in the process.
- [21:34] code-change: Added `.github/dependabot.yml` (github-actions ecosystem, weekly) to keep the SHA-pinned `actions/checkout`/`actions/setup-node` versions from going stale — Dependabot understands SHA pins and opens version-bump PRs. Also extended the pre-commit `lint-staged` glob to cover `.yml`/`.yaml` files, which it previously missed.
- [22:05] code-change: Added `.github/workflows/test.yml` (CLOUD_PLAN.md Phase 3.1's `test-backend`/`test-frontend`/`test-e2e`, `[A]`-scoped) — same trigger/pinning conventions as `lint.yml`. Found and fixed two real bugs while verifying it against a simulated fresh checkout: (1) `.dev.vars` is gitignored, so a fresh clone had no ACCESS_JWKS_URL/AUD for `wrangler dev` to read — e2e job now copies `.dev.vars.example` into place first; (2) the e2e webServer used `vite dev`, whose cold-start JIT compile was flaky against Playwright's default timeout — switched to a build + `vite preview` (needed its own `preview.proxy` config, separate from `server.proxy`), confirmed stable across repeated runs. All three CI jobs pass locally end-to-end; `actionlint` clean.
- [23:01] code-change: Merged `lint.yml` and `test.yml` into a single `.github/workflows/ci.yml` so `test-backend`/`test-frontend` can declare `needs: lint` — `needs` only orders jobs within one workflow file, so two separate PR-triggered workflows had been running fully in parallel with no gating between them, despite the intent. `test-e2e` transitively waits on `lint` via its existing `needs: [test-backend, test-frontend]`. Re-verified full local suite (backend/frontend/e2e) plus `actionlint`/lint/format all clean after the merge.
