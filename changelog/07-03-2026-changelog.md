# Changelog — 07/03/2026

- [06:39] git-commit: Created the repo's initial commit (`37cb116`) — CLOUD_PLAN.md Phase 0/Phase 1 implementation plus lint/format/CI infra, 93 files. Pre-commit hook ran clean (no changes needed).
- [07:00] code-change: Renamed the project from "tasktracker" to "chore-reaper" (user's pick) — root `package.json` name, `backend/wrangler.toml` worker/D1 names, and every markdown reference (`README.md`, `CLOUDFLARE.md`, `CLOUD_PLAN.md`, `TRADEOFFS.md`, `CLAUDE.md`). Regenerated `package-lock.json` and `wrangler types`; reran the full backend/frontend/e2e suite plus lint/format — all clean. Local working directory and any GitHub remote are left untouched — this only covers in-repo naming.
- [07:05] git-push: Added `origin` (https://github.com/GPropersi/chore-reaper), found it already had an initial commit (GPLv3 `LICENSE` only) with unrelated history, merged with `--allow-unrelated-histories` (`d18e3db`), committed the pending rename changes (`6008d4b`), then pushed `main` to origin over SSH (HTTPS had no credential helper entry for github.com in this sandbox; switched the remote to `git@github.com:GPropersi/chore-reaper.git`, which matched an existing SSH identity in `~/.ssh/config`). Branch now tracks `origin/main`, working tree clean.
- [08:26] code-change: Implemented CLOUD_PLAN.md Phase 2 (org-level timezone scoring, per-user display). `useMidnightClock(timezone)` rewritten with `date-fns-tz`; new `ChoresView` wires `GET /api/chores` into `App.tsx`'s previously-placeholder `Home` route (needed for the phase's own e2e test, agreed with user first); `CompletionInfo` now formats in the viewer's personal timezone independent of org-driven scoring. Added `backend/src/seed-mock-chores.ts` + CLI script (mock chore data builder, `bootstrap-admin.ts`-style) per user's direction. All backend (43 tests), frontend (11 tests), and e2e (3 tests, incl. new `timezone-parity.spec.ts`) suites green; `tsc -b` clean. Phase 2's `[A]` checkboxes checked off in `CLOUD_PLAN.md`. Stopped here per instruction for user review — not committed.
- [08:47] git-commit: Committed the Phase 2 work above (`9448e25`, 19 files) at user's request. Pre-commit hook's lint-staged ran ESLint/Prettier auto-fix cleanly; re-verified backend (43) and frontend (11) suites still green post-fix. Not pushed.
- [16:01] plan-review: Explored offline-sync conflict resolution for the upcoming Phase 4 outbox. User raised two concerns (concurrent edits to the same chore while offline; one person on multiple devices) and, offered three options, initially chose full field-level per-field merge. Planned a large backend redesign for it (schema migration, `chores.ts`/routes rewrite, wiring the orphaned `ChoreForm` edit UI) via a Plan-mode design pass with a research + Plan-agent stress test — user pushed back twice, correctly identifying the scope didn't match the actual need and that a migration was pointless pre-deploy (no Cloudflare D1 exists yet, Part B untouched). Landed on a much smaller fix instead; saved a feedback memory on checking scope/deploy-stage before planning around a chosen option's full literal complexity.
- [16:01] code-change: Implemented the small fix — `completeChore` now merges `dateLastCompleted` by keeping the later of two competing completion timestamps instead of rejecting on a stale `version` (never conflicts for completions specifically; `updateChore`/`PUT` and `deleteChore` unchanged). Dropped `expectedVersion` from `completeChore`'s signature and the `version` requirement/409 branch from `PATCH /api/chores/:id/complete`; frontend `ChoresView.handleComplete` no longer sends `version`. TDD throughout (RED tests rewritten first, confirmed failing, then implemented GREEN). Backend (47 tests), frontend (11 tests) green; `tsc -b`/`tsc --noEmit`, ESLint, Prettier all clean. Not committed — awaiting review.
