# Changelog — 07/06/2026

- [06:55] git-change: Completed CLOUD_PLAN.md Phase 0/1.8/1.10/3.2 [B] items end-to-end — real D1, Pages project + custom domain (chores.4irl.app), Zero Trust Access Application, bootstrap admin, and production deploy all live. Checked off corresponding boxes in CLOUD_PLAN.md with root-cause notes (wrong JWKS domain, unscoped Access path).
- [06:55] code-change: Added `[env.production]` D1 binding (remote=true) to backend/wrangler.toml and threaded `WRANGLER_ENV` through bootstrap-admin.ts so one-off scripts can target real D1 without affecting local dev/tests.
- [06:55] code-change: Added `deploy-backend`/`deploy-frontend` jobs to .github/workflows/ci.yml (cloudflare/wrangler-action, gated to push-to-main), a `push` trigger, and a `build:frontend` root script. Checked off Phase 3.1's remaining [B] boxes; left the "merge triggers deploy" verify step and branch-protection ruleset as pending/optional.
- [07:30] code-change: Set up a dedicated GitHub App bot (`c4i-claude-bot`, App ID 4229755) scoped only to GPropersi/chore-reaper, isolated from the `4IRL` org's existing bot. Added `.claude/scripts/generate-gh-token.sh` + `gh-app-push.sh`. Verified end-to-end — pushed branch `test/bot-push-verification` and opened PR #1 as the bot (left open per user's choice, not yet cleaned up).
- [07:34] code-change: Fixed a token-leak in `gh-app-push.sh` — embedding the token in the push URL caused git's own "branch set up to track" message to print it in cleartext. Added `.claude/scripts/gh-app-askpass.sh` (GIT_ASKPASS) so the token is handed to git via env var instead of a URL; re-verified the push prints no secret.
- [07:41] code-change: Extracted the GitHub App bot setup into a reusable global skill at `~/.claude/skills/github-app-bot/SKILL.md` (+ `templates/` for the three scripts), generalizing personal-vs-org repo handling, reuse-vs-new-App decision, and the askpass fix so future repos don't need this re-derived from scratch. Not chore-reaper-specific — lives outside this repo.
- [08:11] code-change: Fixed white page background (`frontend/src/index.css` had no global background — components were individually dark-painted but the canvas wasn't) by setting `html`/`body` to `#111827` matching the PWA manifest's splash color. Also wired up the already-built-but-stubbed Rooms feature: `App.tsx`'s `Layout` was passing `rooms={[]}` and a no-op `onSelect` to `NavBar`; lifted room-selection state to `Layout` and threaded it to `ChoresView` via `Outlet` context, with `ChoresView` reporting its distinct room list back up via a new `onRoomsChange` callback. TDD'd (new tests in `App.test.tsx` and `ChoresView.test.tsx`), verified visually via a temporary e2e Playwright run (screenshots confirmed dark background + working tab filtering, then cleaned up).
- [08:25] code-change: Built a real local-dev flow so the app can be clicked around in an actual browser, not just tested headlessly. Added `backend/scripts/dev-jwt.ts` (+ `npm run dev-jwt`) to mint a long-lived Cloudflare Access JWT signed with the existing e2e fixture key, and made `frontend/vite.config.ts`'s dev proxy auto-attach it from a gitignored `frontend/.env.development.local` (`VITE_DEV_ACCESS_JWT`) — never touches `vite preview`/production. Added a root `npm run dev` (via new `concurrently` devDependency) that launches the JWKS fixture server + backend + frontend together and tears all three down cleanly on one Ctrl-C. Verified end-to-end with curl (no manual headers) getting authenticated `/api/me`/`/api/chores` responses through the proxy, and confirmed SIGINT kills all child processes with no orphaned ports.
- [08:34] code-change: Added project skill `.claude/skills/run-dev/SKILL.md` (`/run-dev`, `/run-dev stop`) to start/stop the local dev stack. Start writes a real OS pidfile to `.claude/run-dev/dev.pid` (gitignored) so stop can find and kill it even from a fresh conversation; stop walks the actual process tree via `pgrep -P` from that pidfile — deliberately not port-based (`lsof -ti :PORT | xargs kill`), after an ad-hoc port-based cleanup command during this same session killed a Firefox tab's content process that happened to hold a socket on one of the dev ports being cleaned up. Verified the safe version end-to-end (start → confirmed process tree is fully scoped to the pidfile's descendants → stop → confirmed zero orphaned listeners and Firefox untouched).
- [08:56] git-push: Bundled this session's work plus pre-existing uncommitted changes (email normalization, CLOUD_PLAN cloud-deploy completion, GitHub App bot setup) into 4 commits on `feat/rooms-ui-and-cloud-deploy`, opened as PR #2 (https://github.com/GPropersi/chore-reaper/pull/2), all pushed/authored as `c4i-claude-bot[bot]` per the user's standing instruction — no personal or Co-Authored-By attribution. Rewrote initial commits' authorship via `git filter-branch --env-filter` (had defaulted to the human + a `Co-Authored-By: Claude` trailer, which showed up on GitHub as a second "claude" co-author) and force-pushed the correction. Added `.claude/skills/bot-push/SKILL.md` encoding the full flow (commit with `--author`, no trailer; `gh-app-push.sh`; `GH_TOKEN`-scoped `gh pr create --head`; verify via `gh pr view --json author,commits`) so this is automatic on future push/PR requests in this repo without re-deriving it.
